---
phase: 01-browser-automation-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - amazon_scraper.py
  - electronics_buyer.py
autonomous: true

must_haves:
  truths:
    - "Task prompts contain no login steps (session handled by persistent profile)"
    - "No except Exception block silently returns NEEDS_PARSING placeholder data"
    - "After agent.run(), the script prints structured OrderDetails JSON with real items and totals (not a placeholder)"
    - "After agent.run(), the script prints structured EBDealResult JSON with a payout value (not a placeholder)"
  artifacts:
    - path: "amazon_scraper.py"
      provides: "AmazonScraper with output_model_schema in both Agent calls"
      contains: "output_model_schema=OrderDetails"
    - path: "amazon_scraper.py"
      provides: "scrape_shipping_confirmation with output_model_schema"
      contains: "output_model_schema=ShippingDetails"
    - path: "electronics_buyer.py"
      provides: "ElectronicsBuyerAgent with same pattern applied"
      contains: "output_model_schema=EBDealResult"
  key_links:
    - from: "amazon_scraper.py Agent()"
      to: "OrderDetails/ShippingDetails Pydantic model"
      via: "output_model_schema parameter"
      pattern: "output_model_schema=Order|output_model_schema=Shipping"
    - from: "agent.run() result"
      to: "structured data"
      via: "result.structured_output or result.get_structured_output(Model)"
      pattern: "structured_output|get_structured_output"
---

<objective>
Fix the four root-cause bugs that make the browser-use Agent crash immediately after opening Amazon. All bugs live in amazon_scraper.py (primary) and electronics_buyer.py (same pattern).

Purpose: Without these fixes, every agent.run() call fails with Pydantic validation errors because browser-use cannot write its DoneAction output — resulting in placeholder NEEDS_PARSING returns or silent crashes.
Output: amazon_scraper.py and electronics_buyer.py with correct structured output wiring, simplified task prompts, and proper error propagation.
</objective>

<execution_context>
@/Users/pinkpanther/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pinkpanther/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add output_model_schema, fix prompts, and fix error handling in amazon_scraper.py</name>
  <files>amazon_scraper.py</files>
  <action>
    Apply all four FIX requirements to amazon_scraper.py:

    FIX-01 — Add output_model_schema to both Agent() calls:
    - In scrape_order_confirmation(): add `output_model_schema=OrderDetails` to the Agent() constructor. OrderDetails is already imported from models.
    - In scrape_shipping_confirmation(): add `output_model_schema=ShippingDetails` to the Agent() constructor. ShippingDetails is already imported from models.

    FIX-02 — Simplify task prompts (remove any login steps):
    - The existing prompts already have no login steps (they go directly to invoice_url and order_details_url). This is correct — leave them as-is. Do NOT add any login instructions.

    FIX-03 and FIX-04 — Replace placeholder returns with real structured output extraction:
    - After each `result = await agent.run()`, replace the entire try/except block and the placeholder return with this pattern:
      ```python
      structured = result.structured_output
      if structured is None:
          raise RuntimeError(f"Agent returned no structured output for order {order_number}. final_result={result.final_result()!r}")
      return structured
      ```
    - This uses result.structured_output (the property on AgentHistoryList) which calls _output_model_schema.model_validate_json(final_result()). It returns None if final_result() is empty — raise a RuntimeError in that case so the caller in browser_agent.py sees a real error rather than placeholder data.
    - Remove the old try/except blocks that call result.extracted_content and return NEEDS_PARSING placeholders entirely.

    Also add debug parameters to both Agent() calls:
    - `generate_gif='./logs/agent.gif'` — records visual action log to ./logs/agent.gif (NOT the default ./agent_history.gif)
    - `save_conversation_path="./logs/agent_conversation.json"` — saves LLM call history
    - `max_failures=5` — increase from default 3 to give agent more attempts
    - `step_timeout=180` — explicitly set (matches current default, makes intent clear)

    The result for scrape_order_confirmation should be:
    ```python
    agent = Agent(
        task=task,
        llm=self.llm,
        use_vision='auto',
        max_actions_per_step=3,
        browser_profile=browser_profile,
        output_model_schema=OrderDetails,
        generate_gif='./logs/agent.gif',
        save_conversation_path="./logs/agent_conversation.json",
        max_failures=5,
        step_timeout=180,
    )
    result = await agent.run()
    structured = result.structured_output
    if structured is None:
        raise RuntimeError(f"Agent returned no structured output for order {order_number}. final_result={result.final_result()!r}")
    return structured
    ```

    Apply the same pattern for scrape_shipping_confirmation with output_model_schema=ShippingDetails (same generate_gif path: './logs/agent.gif').

    Ensure ./logs/ directory exists by adding at the top of the file (after imports):
    ```python
    import os
    os.makedirs("./logs", exist_ok=True)
    ```
  </action>
  <verify>
    Run: `cd /Users/pinkpanther/Desktop/amazon-email-automation && venv/bin/python3 -c "from amazon_scraper import AmazonScraper; print('Import OK')"`
    Expected: "Import OK" with no errors.
    Also check: `grep -n "output_model_schema" amazon_scraper.py` shows two lines (one for OrderDetails, one for ShippingDetails).
    Also check: `grep -n "NEEDS_PARSING" amazon_scraper.py` returns empty (no placeholder returns remain).
    Also check: `grep -n "generate_gif" amazon_scraper.py` shows './logs/agent.gif' (not True).
  </verify>
  <done>
    amazon_scraper.py imports cleanly. Both Agent() calls include output_model_schema. generate_gif is set to './logs/agent.gif'. No NEEDS_PARSING strings remain. result.structured_output is used for data extraction with RuntimeError raised on None.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply the same pattern to electronics_buyer.py</name>
  <files>electronics_buyer.py</files>
  <action>
    Apply the same four fixes to electronics_buyer.py:

    FIX-01 — Add output_model_schema to both Agent() calls:
    - In submit_deal(): add `output_model_schema=EBDealResult` to Agent(). EBDealResult is already imported from models.
    - In submit_tracking(): add `output_model_schema=EBTrackingResult` to Agent(). EBTrackingResult is already imported from models.

    FIX-02 — Simplify task prompts:
    - The existing prompts in electronics_buyer.py DO include login steps (steps 2-3 in both methods: "Check if you're logged in", "If NOT logged in: click Login, enter credentials..."). These must be removed since the persistent browser profile handles session.
    - For submit_deal(), replace the task string with a simplified version that:
      1. Navigates directly to https://electronicsbuyer.gg/app/deals
      2. Searches/scrolls for deals matching the items
      3. Commits deals with the specified quantities
      4. Returns the total payout value
      (Remove steps 2-3 about login entirely)
    - For submit_tracking(), replace the task string with a simplified version that:
      1. Navigates directly to https://electronicsbuyer.gg/app/tracking-submissions
      2. Fills in the form fields (tracking number, package contents)
      3. Submits and returns confirmation
      (Remove steps 2-3 about login entirely)

    FIX-03 and FIX-04 — Replace placeholder returns with structured output:
    - In submit_deal(): after `result = await agent.run()`, replace the try/except and placeholder EBDealResult return with:
      ```python
      structured = result.structured_output
      if structured is None:
          raise RuntimeError(f"Deal submission agent returned no structured output. final_result={result.final_result()!r}")
      return structured
      ```
    - In submit_tracking(): same pattern with EBTrackingResult context in the error message.

    Add debug parameters to both Agent() calls:
    - `generate_gif='./logs/eb_agent.gif'` — records visual action log to ./logs/eb_agent.gif (NOT the default ./agent_history.gif)
    - `save_conversation_path="./logs/eb_agent_conversation.json"`
    - `max_failures=5`

    Add at the top of file (after imports):
    ```python
    import os
    os.makedirs("./logs", exist_ok=True)
    ```
  </action>
  <verify>
    Run: `cd /Users/pinkpanther/Desktop/amazon-email-automation && venv/bin/python3 -c "from electronics_buyer import ElectronicsBuyerAgent; print('Import OK')"`
    Expected: "Import OK" with no errors.
    Also check: `grep -n "output_model_schema" electronics_buyer.py` shows two lines.
    Also check: `grep -n "NEEDS_PARSING" electronics_buyer.py` returns empty.
    Also check: `grep -n "If NOT logged in" electronics_buyer.py` returns empty (login steps removed).
    Also check: `grep -n "generate_gif" electronics_buyer.py` shows './logs/eb_agent.gif' (not True).
  </verify>
  <done>
    electronics_buyer.py imports cleanly. Both Agent() calls include output_model_schema. generate_gif is set to './logs/eb_agent.gif'. No NEEDS_PARSING strings remain. Login steps removed from task prompts. result.structured_output used for data extraction.
  </done>
</task>

</tasks>

<verification>
After both tasks, run the following checks:

1. `cd /Users/pinkpanther/Desktop/amazon-email-automation && venv/bin/python3 -c "from amazon_scraper import AmazonScraper; from electronics_buyer import ElectronicsBuyerAgent; print('All imports OK')"`

2. `grep -n "output_model_schema" /Users/pinkpanther/Desktop/amazon-email-automation/amazon_scraper.py` — must show exactly 2 matches

3. `grep -n "output_model_schema" /Users/pinkpanther/Desktop/amazon-email-automation/electronics_buyer.py` — must show exactly 2 matches

4. `grep -rn "NEEDS_PARSING" /Users/pinkpanther/Desktop/amazon-email-automation/*.py` — must return empty

5. `grep -rn "If NOT logged in" /Users/pinkpanther/Desktop/amazon-email-automation/electronics_buyer.py` — must return empty

6. `grep -n "generate_gif" /Users/pinkpanther/Desktop/amazon-email-automation/amazon_scraper.py` — must show './logs/agent.gif' (not True)

7. `grep -n "generate_gif" /Users/pinkpanther/Desktop/amazon-email-automation/electronics_buyer.py` — must show './logs/eb_agent.gif' (not True)
</verification>

<success_criteria>
Both files import without errors. output_model_schema is set on all 4 Agent() instantiations (2 in amazon_scraper.py, 2 in electronics_buyer.py). generate_gif is set to named file paths ('./logs/agent.gif' and './logs/eb_agent.gif') not True. Zero NEEDS_PARSING placeholder strings remain in any .py file. result.structured_output is used for data extraction with RuntimeError on empty output. Login steps are removed from electronics_buyer.py task prompts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-browser-automation-fix/01-01-SUMMARY.md` following the summary template.
</output>
