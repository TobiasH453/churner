---
phase: 04-smoke-verification-harness
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - scripts/verify-smoke-readiness.sh
  - scripts/smoke-validate-response.py
  - docs/SMOKE_VERIFICATION.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - Smoke harness verifies order-confirmation path through real `/process-order` API invocation.
    - Order stage pass criteria include structured response validation, not only HTTP success.
    - Order-stage inputs are deterministic and operator-configurable without embedding secret data.
  artifacts:
    - scripts/verify-smoke-readiness.sh
    - scripts/smoke-validate-response.py
    - docs/SMOKE_VERIFICATION.md
  key_links:
    - Response checks reuse project contract model definitions from `models.py`.
    - Order-stage behavior aligns with Phase 4 context decision for transport-then-contract validation.
---

<objective>
Add order-confirmation contract-path verification to the smoke harness.

Purpose: Satisfy VER-02 by validating functional order path readiness through the API contract boundary.
Output: Smoke order stage implementation, reusable response validator, and operator guidance for order fixture inputs.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-smoke-verification-harness/04-CONTEXT.md
@.planning/phases/04-smoke-verification-harness/04-RESEARCH.md
@.planning/phases/04-smoke-verification-harness/04-01-SUMMARY.md
@scripts/verify-smoke-readiness.sh
@models.py
@main.py
@browser_agent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic order smoke stage in runner</name>
  <files>scripts/verify-smoke-readiness.sh</files>
  <action>Implement `order_confirmation` stage that POSTs a deterministic payload to `/process-order` with bounded timeouts and response capture. Require explicit smoke input source (env vars or fixture file) and clear failures when required inputs are absent.</action>
  <verify>Run smoke command in a valid-input path and in a missing-input path; confirm pass/fail cues and remediation are explicit.</verify>
  <done>Order stage executes predictably and provides actionable operator output.</done>
</task>

<task type="auto">
  <name>Task 2: Implement structured response validation helper</name>
  <files>scripts/smoke-validate-response.py</files>
  <action>Create a Python helper that validates API response JSON against `AgentResponse` contract semantics and expected `email_type`. Fail with descriptive messages when schema or key invariants are violated.</action>
  <verify>Run helper against representative pass/fail JSON samples; confirm non-zero exit on invalid contract shapes or mismatched `email_type`.</verify>
  <done>Order stage pass criteria are contract-aware and deterministic.</done>
</task>

<task type="auto">
  <name>Task 3: Document order-stage smoke input/validation rules</name>
  <files>docs/SMOKE_VERIFICATION.md</files>
  <action>Publish operator-facing smoke guide section for order path: required input fields, command examples, expected output cues, and how to interpret contract validation failures.</action>
  <verify>Cross-check docs with script flags/env names and validator behavior so examples are executable as written.</verify>
  <done>Order smoke validation is documented with copy/paste-ready operator flow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash -n scripts/verify-smoke-readiness.sh`
- [ ] `python3 scripts/smoke-validate-response.py --help` (or equivalent usage output) succeeds
- [ ] Smoke order stage validates `email_type=order_confirmation` response contract path
- [ ] `docs/SMOKE_VERIFICATION.md` covers order input, command, and pass/fail interpretation
</verification>

<success_criteria>

- All tasks completed
- VER-02 path is executable and contract-validated
- Smoke harness now checks health + order with deterministic operator guidance
</success_criteria>

<output>
After completion, create `.planning/phases/04-smoke-verification-harness/04-02-SUMMARY.md`
</output>
