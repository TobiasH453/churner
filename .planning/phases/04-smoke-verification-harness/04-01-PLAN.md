---
phase: 04-smoke-verification-harness
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-smoke-readiness.sh
  - scripts/service-env.sh
  - docs/RUNTIME_VALIDATION.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - Operator has one canonical smoke-test command entrypoint under `scripts/` that runs from repo root.
    - Smoke harness output contract matches existing operator script conventions (`[INFO]`, `[PASS]`, `[WARN]`, `[FAIL]`, `Next:`).
    - Smoke runner enforces deterministic stage execution and non-zero exit on blocking failures.
  artifacts:
    - scripts/verify-smoke-readiness.sh
    - docs/RUNTIME_VALIDATION.md
  key_links:
    - Smoke command shape aligns with runtime command UX defined in `docs/OPERATIONS.md`.
    - Baseline stage framework enables downstream order/shipping contract checks in later Phase 4 plans.
---

<objective>
Build the Phase 4 smoke verification runner and lock output semantics.

Purpose: Establish one command operators can run after install to begin functional readiness verification.
Output: New smoke runner script with deterministic stage orchestration and documented command contract.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-smoke-verification-harness/04-CONTEXT.md
@.planning/phases/04-smoke-verification-harness/04-RESEARCH.md
@scripts/service-env.sh
@scripts/services-status.sh
@scripts/verify-runtime-operations.sh
@main.py
@docs/INSTALL.md
@docs/RUNTIME_VALIDATION.md
@docs/OPERATIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create smoke runner entrypoint with shared script contract</name>
  <files>scripts/verify-smoke-readiness.sh</files>
  <action>Create a bash smoke runner that uses strict shell options, dependency checks, and stage helpers consistent with existing scripts. Include usage output and deterministic non-interactive behavior.</action>
  <verify>Run `bash -n scripts/verify-smoke-readiness.sh` and confirm helper labels/output style mirror runtime scripts.</verify>
  <done>Smoke runner entrypoint exists and is shell-valid with standardized operator UX.</done>
</task>

<task type="auto">
  <name>Task 2: Implement baseline health stage and stage-gating behavior</name>
  <files>scripts/verify-smoke-readiness.sh, scripts/service-env.sh</files>
  <action>Implement stage order scaffolding with explicit `health` check first (`GET /health`) using bounded timeouts and body-preserving failures. Enforce stop-on-health-fail behavior with actionable `Next:` remediation.</action>
  <verify>Run script against a running stack and against a down stack; verify health pass/fail cues and non-zero exit behavior on blocking failure.</verify>
  <done>Smoke runner has reliable stage gating and health verification as first required signal.</done>
</task>

<task type="auto">
  <name>Task 3: Document smoke command contract and invocation path</name>
  <files>docs/RUNTIME_VALIDATION.md</files>
  <action>Add Phase 4 handoff documentation for the smoke command, expected pass/fail cues, and where contract-path checks are added next.</action>
  <verify>Ensure docs include exact command path, expected result markers, and no conflicts with existing runtime validation flow.</verify>
  <done>Operators have clear initial guidance for running smoke verification after runtime checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash -n scripts/verify-smoke-readiness.sh`
- [ ] Smoke runner has standardized output labels and `Next:` remediation style
- [ ] Health stage checks `http://localhost:${SERVER_PORT:-18080}/health` with explicit timeout bounds
- [ ] `docs/RUNTIME_VALIDATION.md` includes smoke command handoff
</verification>

<success_criteria>

- All tasks completed
- One-command smoke entrypoint is established with deterministic baseline behavior
- VER-01 groundwork is in place for full contract-path validation
</success_criteria>

<output>
After completion, create `.planning/phases/04-smoke-verification-harness/04-01-SUMMARY.md`
</output>
