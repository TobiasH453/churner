---
phase: 06-distribution-release-readiness
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-release-bundle.sh
  - docs/RELEASE.md
  - .gitignore
autonomous: true
user_setup: []
must_haves:
  truths:
    - Operator can build one Apple Silicon macOS `.zip` release artifact named with `vX.Y.Z` semantics.
    - The release bundle contains the required runtime code, scripts, docs, workflow artifacts, `.env.example`, and a generated non-secret `.env` template, while excluding tests and sensitive local state.
    - Release packaging reuses the existing bundle audit policy and fails hard when forbidden paths or missing required artifacts are detected.
  artifacts:
    - scripts/build-release-bundle.sh
    - docs/RELEASE.md
    - .gitignore
  key_links:
    - Packaging must enforce `.bundleignore` through `bash scripts/audit-bundle.sh`.
    - Release contents must preserve the existing operator command path documented across install, runtime, n8n, smoke, and diagnostics docs.
---

<objective>
Define the Phase 6 bundle structure and implement deterministic packaging steps.

Purpose: Satisfy DIST-01 by producing one releasable macOS-first archive that includes the required product artifacts and excludes sensitive local state by default.
Output: `scripts/build-release-bundle.sh`, release packaging documentation, and any ignore/output-path updates needed for safe local generation.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-distribution-release-readiness/06-CONTEXT.md
@.planning/phases/06-distribution-release-readiness/06-RESEARCH.md
@README.md
@docs/INSTALL.md
@docs/OPERATIONS.md
@docs/N8N_INTEGRATION.md
@docs/PACKAGING_BASELINE.md
@.bundleignore
@.gitignore
@scripts/audit-bundle.sh
@n8n-workflows/03-process-order-v1.0.0.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement a deterministic release bundle builder</name>
  <files>scripts/build-release-bundle.sh</files>
  <action>Create a non-interactive packaging entrypoint that stages the Phase 6 release into a clean output directory and emits one Apple Silicon `.zip` artifact named with `vX.Y.Z`. Require an explicit version input, include the locked release contents, generate a non-secret staged `.env` template alongside `.env.example`, and fail if required files are missing.</action>
  <verify>Run `bash -n scripts/build-release-bundle.sh`; run `bash scripts/build-release-bundle.sh --help`; run a local packaging pass such as `RELEASE_VERSION=v1.0.0 bash scripts/build-release-bundle.sh --output-dir /tmp/phase6-release` and confirm the staged directory plus `.zip` are created with deterministic naming.</verify>
  <done>Packaging can be executed with one command and produces a predictable release artifact structure for v1 distribution.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce release contents and exclusion rules through the existing audit contract</name>
  <files>scripts/build-release-bundle.sh, .gitignore</files>
  <action>Build the release from an explicit include set that contains app code, scripts, docs, and `n8n-workflows/`, while excluding tests, local caches, logs, diagnostics outputs, virtual environments, browser profiles, and other forbidden local state. Run `bash scripts/audit-bundle.sh` against the staged file list or generated archive before reporting success, and ensure generated release outputs stay ignored locally.</action>
  <verify>Inspect the staged tree and zip contents; run `bash scripts/audit-bundle.sh --archive /tmp/phase6-release/<artifact>.zip`; confirm audit passes and that no forbidden paths from `.bundleignore` or tests appear in the release.</verify>
  <done>The packaging path is safe-by-default and blocks unsafe or incomplete release candidates.</done>
</task>

<task type="auto">
  <name>Task 3: Document bundle structure and packaging operator steps</name>
  <files>docs/RELEASE.md</files>
  <action>Author release packaging documentation that states the supported platform (Apple Silicon), artifact naming/version contract, exact packaging command, required contents, hard exclusions, output location, and how the shared-cloud distribution bundle should be prepared for users. Make clear that the workflow JSON is bundled as a provided artifact and that secrets/session state are never shipped.</action>
  <verify>Read `docs/RELEASE.md` end-to-end and confirm it answers: what gets packaged, what is excluded, how to build the zip, and what file is intended to be shared.</verify>
  <done>Maintainers have a canonical packaging reference that matches the generated release artifact.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash -n scripts/build-release-bundle.sh`
- [ ] `bash scripts/build-release-bundle.sh --help` returns usage text and exit code `0`
- [ ] A local release build succeeds with explicit version input and emits one `.zip` artifact in a deterministic output directory
- [ ] Generated release contents include required docs/scripts/workflow files plus `.env.example` and a staged non-secret `.env` template
- [ ] Generated release contents exclude tests and all forbidden local state from `.bundleignore`
- [ ] `bash scripts/audit-bundle.sh --archive <generated-zip>` passes
- [ ] `docs/RELEASE.md` matches the implemented packaging command and content policy
</verification>

<success_criteria>

- All tasks completed
- DIST-01 is satisfied with a reproducible, audit-gated release bundle path
- Phase 6 now has a concrete bundle structure and packaging command for v1 distribution
</success_criteria>

<output>
After completion, create `.planning/phases/06-distribution-release-readiness/06-01-SUMMARY.md`
</output>
