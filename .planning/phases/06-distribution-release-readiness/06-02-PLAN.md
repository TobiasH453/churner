---
phase: 06-distribution-release-readiness
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - README.md
  - docs/RELEASE.md
  - docs/RELEASE_CHECKLIST.md
  - scripts/verify-release-readiness.sh
autonomous: true
user_setup: []
must_haves:
  truths:
    - Package documentation clearly states prerequisites, included files, user-provided items, and the exact first-run sequence from `README.md`.
    - Release readiness is gated by a strict checklist covering `install -> run -> smoke verify -> diagnostics`, with any failed required command or missing artifact blocking shipment.
    - Release docs QA validates that the packaging output and operator guidance stay aligned with the current script/document command surface.
  artifacts:
    - README.md
    - docs/RELEASE.md
    - docs/RELEASE_CHECKLIST.md
    - scripts/verify-release-readiness.sh
  key_links:
    - `README.md` must remain the canonical first-run entrypoint and hand off to detailed docs without conflicting instructions.
    - Release verification must reuse existing runtime, smoke, diagnostics, and bundle-audit commands rather than inventing alternative flows.
---

<objective>
Finalize the release checklist and perform documentation QA for the Phase 6 distribution path.

Purpose: Satisfy DIST-02 and the phase release-goal checks by making the download package understandable, runnable, and strictly gated before sharing.
Output: Canonical first-run release docs in `README.md`, a strict release checklist document, and a docs/readiness verification helper.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-distribution-release-readiness/06-CONTEXT.md
@.planning/phases/06-distribution-release-readiness/06-RESEARCH.md
@.planning/phases/06-distribution-release-readiness/06-01-SUMMARY.md
@README.md
@docs/INSTALL.md
@docs/OPERATIONS.md
@docs/N8N_INTEGRATION.md
@docs/SMOKE_VERIFICATION.md
@docs/TROUBLESHOOTING.md
@docs/RELEASE.md
@scripts/audit-bundle.sh
@scripts/services-up.sh
@scripts/services-status.sh
@scripts/verify-runtime-operations.sh
@scripts/verify-n8n-workflow-contract.sh
@scripts/verify-smoke-readiness.sh
@scripts/collect-diagnostics.sh
@scripts/build-release-bundle.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make `README.md` the canonical first-run release path</name>
  <files>README.md, docs/RELEASE.md</files>
  <action>Rewrite the release-facing README so it starts with prerequisites, states what the download includes, states what the user must provide locally, and gives a strict copy/paste command sequence for first run. Keep `README.md` canonical, with clear handoffs to detailed docs for n8n JSON import, troubleshooting, and release packaging specifics.</action>
  <verify>Read `README.md` top-to-bottom and confirm a new user can identify prerequisites, included files, required local inputs, and the exact command order without leaving the page prematurely.</verify>
  <done>The release bundle has one clear front door for semi-technical users.</done>
</task>

<task type="auto">
  <name>Task 2: Create a strict release checklist with hard blockers</name>
  <files>docs/RELEASE_CHECKLIST.md</files>
  <action>Create a checklist document that records release version/date, required artifact presence, bundle audit result, and the required go/no-go validation flow: install, services up/status, runtime verification, smoke verification, and diagnostics collection. Mark any failed required command or missing required artifact as an immediate no-ship condition.</action>
  <verify>Review the checklist and confirm every item is binary, version/date evidence is captured, and the required flow matches the locked phase context exactly.</verify>
  <done>Maintainers have a release gate they can run without ambiguity or exceptions.</done>
</task>

<task type="auto">
  <name>Task 3: Add release docs QA automation tied to the real command surface</name>
  <files>scripts/verify-release-readiness.sh, README.md, docs/RELEASE.md, docs/RELEASE_CHECKLIST.md</files>
  <action>Implement a verification helper that checks for the presence of required release docs/artifacts, validates that canonical commands referenced in release docs exist, and confirms the release package/checklist still point to the correct install/runtime/smoke/diagnostics flow. Keep the helper deterministic and non-interactive.</action>
  <verify>Run `bash -n scripts/verify-release-readiness.sh`; run `bash scripts/verify-release-readiness.sh`; confirm it returns success only when required files and command references are present and current.</verify>
  <done>Release documentation drift can be caught with one reproducible verification command before shipping.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `README.md` starts with prerequisites and includes a strict first-run command sequence
- [ ] `README.md` explicitly states what is included in the download and what users must provide locally
- [ ] `docs/RELEASE_CHECKLIST.md` records version/date and uses strict go/no-go blockers
- [ ] The checklist explicitly covers `install -> run -> smoke verify -> diagnostics`
- [ ] `bash -n scripts/verify-release-readiness.sh`
- [ ] `bash scripts/verify-release-readiness.sh` passes
- [ ] Release docs reference only real commands/files present in the repository and release bundle
</verification>

<success_criteria>

- All tasks completed
- DIST-02 is satisfied with canonical release-facing documentation and a strict release checklist
- Phase 6 release QA can be rerun deterministically before sharing the download bundle
</success_criteria>

<output>
After completion, create `.planning/phases/06-distribution-release-readiness/06-02-SUMMARY.md`
</output>
