---
phase: 06-distribution-release-readiness
plan: "04"
type: execute
wave: 2
depends_on:
  - "06-03"
gap_closure: true
files_modified:
  - scripts/build-release-bundle.sh
  - scripts/verify-release-readiness.sh
  - docs/RELEASE.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .planning/phases/01-installer-secure-bootstrap/01-UAT.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - Release packaging no longer depends on untracked runtime files from a dirty workspace; the release build is reproducible from committed repository state or explicitly blocks otherwise.
    - Release verification fails when required ship artifacts are untracked or otherwise absent from committed source.
    - Planning metadata is normalized after the release blockers are fixed: requirements completion view is coherent, milestone decisions are no longer left as pending, and stale UAT state is no longer misleading.
  artifacts:
    - scripts/build-release-bundle.sh
    - scripts/verify-release-readiness.sh
    - docs/RELEASE.md
    - .planning/REQUIREMENTS.md
    - .planning/PROJECT.md
    - .planning/STATE.md
    - .planning/phases/01-installer-secure-bootstrap/01-UAT.md
  key_links:
    - Gap source: `.planning/v1-MILESTONE-AUDIT.md` reproducibility blocker and metadata drift notes
    - Metadata normalization should happen only after the release-source-state blocker is concretely addressed
---

<objective>
Restore release reproducibility from committed source state, then normalize milestone metadata.

Purpose: Close the audit blocker where Phase 6 packaging succeeds only from the current dirty worktree, and clean up stale planning metadata once the release gate is trustworthy again.
Output: release builder/readiness checks that enforce committed-source reproducibility, plus normalized milestone metadata files.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1-MILESTONE-AUDIT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/06-distribution-release-readiness/06-VERIFICATION.md
@scripts/build-release-bundle.sh
@scripts/verify-release-readiness.sh
@docs/RELEASE.md
@.planning/phases/01-installer-secure-bootstrap/01-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eliminate dirty-worktree release inputs</name>
  <files>scripts/build-release-bundle.sh, scripts/verify-release-readiness.sh, docs/RELEASE.md</files>
  <action>Resolve the release reproducibility blocker by making the release build depend only on committed ship artifacts. If a required runtime file is currently untracked, either bring the canonical file into committed source as part of the fix or make the release build/readiness checks fail fast with explicit remediation until the repository state is corrected. The final release path must not silently package untracked local files.</action>
  <verify>Use `git ls-files` or equivalent checks in the release verification path; run the build/readiness commands and confirm they only pass when all required ship artifacts are in committed source state.</verify>
  <done>Phase 6 packaging is trustworthy from repository state, not just from one local workspace.</done>
</task>

<task type="auto">
  <name>Task 2: Add reproducibility checks to the release gate</name>
  <files>scripts/verify-release-readiness.sh, docs/RELEASE.md</files>
  <action>Teach the release QA path to verify committed-source reproducibility explicitly, and document the clean build expectation in `docs/RELEASE.md`. Keep the failure mode concrete so maintainers know exactly which files or conditions block shipment.</action>
  <verify>Run `bash -n scripts/verify-release-readiness.sh`; run `bash scripts/verify-release-readiness.sh`; confirm it fails on missing/untracked required ship files and passes once the release input set is valid.</verify>
  <done>Release readiness checks now enforce source-state integrity instead of only archive contents.</done>
</task>

<task type="auto">
  <name>Task 3: Normalize milestone metadata after the release blockers are fixed</name>
  <files>.planning/REQUIREMENTS.md, .planning/PROJECT.md, .planning/STATE.md, .planning/phases/01-installer-secure-bootstrap/01-UAT.md</files>
  <action>Clean up the stale planning metadata identified by the milestone audit: make the top-level v1 requirements checklist coherent with traceability/completion, replace stale `Pending` decision outcomes in `PROJECT.md` with current reality where appropriate, and close or clearly mark the stale Phase 1 UAT session so future verify-work runs are not misled.</action>
  <verify>Cross-read the normalized files and confirm milestone status, requirement completion signals, and residual UAT state all tell the same story with no stale pending markers for completed work.</verify>
  <done>Project metadata reflects the real post-gap milestone state instead of a mix of old pending markers and newer completion tables.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Release build no longer packages required files that are absent from committed source state
- [ ] `bash -n scripts/verify-release-readiness.sh`
- [ ] `bash scripts/verify-release-readiness.sh` enforces committed-source reproducibility
- [ ] `docs/RELEASE.md` documents the committed-source expectation for shipping
- [ ] `.planning/REQUIREMENTS.md`, `.planning/PROJECT.md`, and `.planning/STATE.md` present coherent milestone completion metadata
- [ ] Stale Phase 1 UAT session is closed or clearly marked non-active
</verification>

<success_criteria>

- Milestone audit gap for DIST-01 is closed
- Release packaging is reproducible from committed source state
- Metadata drift called out in the milestone audit is substantially normalized
</success_criteria>

<output>
After completion, create `.planning/phases/06-distribution-release-readiness/06-04-SUMMARY.md`
</output>
