---
phase: 06-distribution-release-readiness
plan: "03"
type: execute
wave: 1
depends_on:
  - "06-02"
gap_closure: true
files_modified:
  - README.md
  - docs/INSTALL.md
  - docs/ENVIRONMENT.md
  - install.sh
  - scripts/validate-env.sh
  - scripts/verify-release-readiness.sh
autonomous: true
user_setup: []
must_haves:
  truths:
    - The canonical first-run flow no longer tells users to run `install.sh` before the placeholder `.env` is in a valid state for the installer's own validation rules.
    - Installer/bootstrap behavior and release-facing docs converge on one env-edit/validation sequence with no contradictory steps.
    - Release docs QA fails if README, install flow, and env validation behavior drift apart again.
  artifacts:
    - README.md
    - docs/INSTALL.md
    - docs/ENVIRONMENT.md
    - install.sh
    - scripts/validate-env.sh
    - scripts/verify-release-readiness.sh
  key_links:
    - Gap source: `.planning/v1-MILESTONE-AUDIT.md` release flow blocker
    - Release docs must stay aligned with the Phase 1 installer contract rather than overriding it informally
---

<objective>
Repair the broken release first-run flow by aligning installer behavior, env validation, and release-facing docs.

Purpose: Close the milestone audit gap where the shipped README tells users to run `install.sh` before editing `.env`, even though installer bootstrap validates placeholder values immediately.
Output: one canonical env/setup sequence implemented across installer behavior and user-facing docs, plus QA checks that enforce it.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1-MILESTONE-AUDIT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-distribution-release-readiness/06-VERIFICATION.md
@README.md
@docs/INSTALL.md
@docs/ENVIRONMENT.md
@install.sh
@scripts/validate-env.sh
@scripts/verify-release-readiness.sh
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Choose and implement one canonical `.env` sequence</name>
  <files>install.sh, scripts/validate-env.sh, README.md, docs/INSTALL.md, docs/ENVIRONMENT.md</files>
  <action>Resolve the installer/docs contradiction by implementing exactly one supported flow for `.env` editing and validation. The final behavior must be explicit and safe: either the installer becomes placeholder-tolerant until the user edits `.env`, or the docs move `.env` editing/validation ahead of installer execution. Do not leave a mixed contract across files.</action>
  <verify>Perform a local replay against a non-secret placeholder env state and confirm the documented order matches actual installer/validator behavior, including pass/fail cues and next-step guidance.</verify>
  <done>User-facing setup instructions and installer behavior agree on when `.env` must be edited and when validation occurs.</done>
</task>

<task type="auto">
  <name>Task 2: Update release-facing docs to the repaired flow</name>
  <files>README.md, docs/INSTALL.md, docs/ENVIRONMENT.md</files>
  <action>Rewrite the affected docs so they all describe the same env/bootstrap sequence, including what ships in the release bundle, what the user edits locally, and what command to run next after each step. Remove any stale wording that implies placeholder `.env` values will pass installer validation unchanged.</action>
  <verify>Cross-read the three docs and confirm they no longer disagree on install order, env editing timing, or validation timing.</verify>
  <done>README/install/environment docs present one deterministic onboarding path with no contradictory steps.</done>
</task>

<task type="auto">
  <name>Task 3: Extend release QA to guard the sequencing contract</name>
  <files>scripts/verify-release-readiness.sh, README.md, docs/INSTALL.md, docs/ENVIRONMENT.md</files>
  <action>Strengthen release QA so it checks the canonical setup order and fails if README, install docs, and env docs drift apart again. Keep checks deterministic and rooted in the actual supported command sequence.</action>
  <verify>Run `bash -n scripts/verify-release-readiness.sh`; run `bash scripts/verify-release-readiness.sh`; confirm it fails on sequencing drift and passes on the repaired flow.</verify>
  <done>The audit blocker is covered by an executable docs/behavior consistency gate.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Installer/env validation behavior matches the documented first-run order
- [ ] `README.md`, `docs/INSTALL.md`, and `docs/ENVIRONMENT.md` describe the same env/edit/validate/install sequence
- [ ] `bash -n scripts/verify-release-readiness.sh`
- [ ] `bash scripts/verify-release-readiness.sh` passes
- [ ] No release-facing doc still instructs users to run an invalid placeholder flow
</verification>

<success_criteria>

- Milestone audit gap for DIST-02 is closed
- The release onboarding path is executable as documented
- Sequencing drift is guarded by deterministic release QA
</success_criteria>

<output>
After completion, create `.planning/phases/06-distribution-release-readiness/06-03-SUMMARY.md`
</output>
