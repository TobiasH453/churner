---
phase: 03-n8n-workflow-packaging-contract-alignment
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - n8n-workflows/03-process-order-v1.0.0.json
  - n8n-workflows/README.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - Operator can import a versioned workflow artifact from `n8n-workflows/` without manual JSON edits.
    - Workflow artifact includes deterministic trigger, normalization, and API call stages needed for `/process-order` flow.
    - Artifact versioning is explicit so downstream docs/tests can refer to one canonical file.
  artifacts:
    - n8n-workflows/03-process-order-v1.0.0.json
    - n8n-workflows/README.md
  key_links:
    - Workflow filename uses SemVer and is stable for docs and validation checks.
    - Artifact schema is import-ready JSON generated for n8n workflow import.
---

<objective>
Create the canonical, versioned n8n workflow artifact for Phase 3.

Purpose: Establish one distributable workflow JSON that operators can import reproducibly.
Output: Importable `n8n-workflows/03-process-order-v1.0.0.json` plus versioning notes in `n8n-workflows/README.md`.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-n8n-workflow-packaging-contract-alignment/03-CONTEXT.md
@.planning/phases/03-n8n-workflow-packaging-contract-alignment/03-RESEARCH.md
@main.py
@models.py
@docs/INSTALL.md
@docs/OPERATIONS.md
@n8n-workflows/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create canonical versioned workflow JSON artifact</name>
  <files>n8n-workflows/03-process-order-v1.0.0.json</files>
  <action>Author/import-export a valid n8n workflow JSON artifact that includes trigger, payload normalization, required-field guard checks, and HTTP Request to `/process-order`. Ensure the workflow can be imported without manual JSON patching.</action>
  <verify>Validate JSON syntax (`python -m json.tool n8n-workflows/03-process-order-v1.0.0.json >/dev/null`) and inspect for required nodes/fields (`rg` checks for `email_type`, `order_number`, `account_type`, `/process-order`).</verify>
  <done>Canonical Phase 3 workflow artifact exists as valid importable JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Encode versioning and artifact usage contract</name>
  <files>n8n-workflows/README.md</files>
  <action>Document naming/versioning rules for workflow artifacts (SemVer in filename, update behavior for minor/patch changes) and identify the canonical artifact for Phase 3 operators.</action>
  <verify>Check README references exact artifact path and includes import prerequisites + version bump rules.</verify>
  <done>Workflow artifact versioning contract is explicit and future-safe.</done>
</task>

<task type="auto">
  <name>Task 3: Align artifact defaults with Phase 3 decisions</name>
  <files>n8n-workflows/03-process-order-v1.0.0.json</files>
  <action>Ensure payload behavior in workflow matches decisions from `03-CONTEXT.md`: default `account_type` to personal when missing, allow unknown fields with warning path, and preserve compatibility warning hooks for minor contract variants.</action>
  <verify>Inspect workflow field/default expressions and warning branch outputs; confirm no required field depends on manual operator patching.</verify>
  <done>Artifact behavior is aligned with locked Phase 3 payload decisions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m json.tool n8n-workflows/03-process-order-v1.0.0.json >/dev/null`
- [ ] Workflow JSON includes `email_type`, `order_number`, `account_type` handling and `/process-order` HTTP target
- [ ] `n8n-workflows/README.md` defines canonical artifact and SemVer naming rule
</verification>

<success_criteria>

- All tasks completed
- Workflow artifact is versioned, importable, and decision-aligned
- Artifact contract is documented for downstream validation and docs plans
</success_criteria>

<output>
After completion, create `.planning/phases/03-n8n-workflow-packaging-contract-alignment/03-01-SUMMARY.md`
</output>
