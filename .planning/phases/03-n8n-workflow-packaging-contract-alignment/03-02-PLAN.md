---
phase: 03-n8n-workflow-packaging-contract-alignment
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - test_n8n_process_order_contract.py
  - scripts/verify-n8n-workflow-contract.sh
  - docs/N8N_PAYLOAD_CONTRACT.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - Imported workflow payload behavior is checked against `/process-order` contract without relying on manual review.
    - Required-field handling, optional `account_type` defaulting, and unknown-field warning behavior are codified and testable.
    - Contract notes explain compatibility expectations and warning semantics for minor variants.
  artifacts:
    - test_n8n_process_order_contract.py
    - scripts/verify-n8n-workflow-contract.sh
    - docs/N8N_PAYLOAD_CONTRACT.md
  key_links:
    - Contract verification reads canonical workflow artifact produced by `03-01`.
    - Notes and checks align to `models.EmailData` and Phase 3 locked context decisions.
---

<objective>
Add executable payload-contract verification and alignment notes for the n8n workflow artifact.

Purpose: Prove imported workflow payloads match API expectations and Phase 3 decision rules.
Output: Contract test + runner script + operator-readable contract alignment notes.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-n8n-workflow-packaging-contract-alignment/03-CONTEXT.md
@.planning/phases/03-n8n-workflow-packaging-contract-alignment/03-RESEARCH.md
@.planning/phases/03-n8n-workflow-packaging-contract-alignment/03-01-SUMMARY.md
@n8n-workflows/03-process-order-v1.0.0.json
@models.py
@main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement static contract checks for workflow payload mapping</name>
  <files>test_n8n_process_order_contract.py</files>
  <action>Create a fast static contract test that parses workflow JSON and asserts required `/process-order` payload mapping: required fields present, `account_type` default behavior, and warning-path hooks for unknown fields/compatibility mode.</action>
  <verify>Run `python test_n8n_process_order_contract.py` and confirm PASS output; include failure messages that identify missing mapping/default contracts.</verify>
  <done>Workflow contract behavior is enforced by executable checks.</done>
</task>

<task type="auto">
  <name>Task 2: Add one-command contract verification wrapper</name>
  <files>scripts/verify-n8n-workflow-contract.sh</files>
  <action>Add a shell wrapper that validates workflow JSON syntax and runs `test_n8n_process_order_contract.py` with clear PASS/FAIL output and non-zero exit on blocking contract failures.</action>
  <verify>Run `bash -n scripts/verify-n8n-workflow-contract.sh` then execute script in pass path; confirm deterministic exit code and actionable failure hint text.</verify>
  <done>Operator/dev can validate workflow contract with one command.</done>
</task>

<task type="auto">
  <name>Task 3: Publish payload contract alignment notes</name>
  <files>docs/N8N_PAYLOAD_CONTRACT.md</files>
  <action>Document the exact payload contract for n8n -> `/process-order`, including required fields, optional/default fields, unknown-field warning policy, and compatibility warning behavior for minor variants.</action>
  <verify>Cross-check documented fields against `models.py`, workflow JSON, and contract test expectations.</verify>
  <done>Contract notes provide a single source of truth for payload semantics and checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python test_n8n_process_order_contract.py`
- [ ] `bash -n scripts/verify-n8n-workflow-contract.sh`
- [ ] `bash scripts/verify-n8n-workflow-contract.sh` returns zero on pass and non-zero on blocking mismatch
- [ ] `docs/N8N_PAYLOAD_CONTRACT.md` aligns with `models.py` + workflow behavior
</verification>

<success_criteria>

- All tasks completed
- Payload contract behavior is automatically verifiable
- Alignment notes make Phase 3 decision rules explicit and auditable
</success_criteria>

<output>
After completion, create `.planning/phases/03-n8n-workflow-packaging-contract-alignment/03-02-SUMMARY.md`
</output>
