---
phase: 03-electronicsbuyer-submission-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - models.py
  - electronics_buyer.py
  - browser_agent.py
  - test_eb_tracking_contract.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Tracking submission only returns success after required fields are filled and a concrete submit-success signal is observed"
    - "Duplicate tracking submissions are blocked and explicitly flagged"
    - "Tracking submission runtime remains bounded (no long retry loops, max two attempts)"
  artifacts:
    - path: "electronics_buyer.py"
      provides: "Deterministic tracking modal resolution, duplicate detection, and explicit failure classification"
      contains: "submit_tracking"
    - path: "models.py"
      provides: "Result schema fields that can carry warning/flag context"
      contains: "EBTrackingResult"
    - path: "test_eb_tracking_contract.py"
      provides: "Fast contract check for tracking outcome classification"
      contains: "FLAG_DUPLICATE_TRACKING"
  key_links:
    - from: "electronics_buyer.py"
      to: "browser_agent.py"
      via: "EBTrackingResult.error_message/warnings propagation"
      pattern: "submit_tracking -> AgentResponse"
---

<objective>
Harden deterministic ElectronicsBuyer tracking submission so modal interaction failures and duplicate submissions become explicit, bounded, and diagnosable.

Purpose: Close the highest-impact Phase 3 failure mode (tracking modal/input instability) without expanding scope into new features.
Output: Robust tracking submit path, explicit failure/flag contract, and a fast regression check script.
</objective>

<execution_context>
@./get-shit-done/workflows/execute-plan.md
@./get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-electronicsbuyer-submission-hardening/03-CONTEXT.md
@.planning/phases/03-electronicsbuyer-submission-hardening/03-RESEARCH.md
@electronics_buyer.py
@browser_agent.py
@models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen tracking modal discovery and required-field fill contract</name>
  <files>electronics_buyer.py</files>
  <action>
    Replace fragile modal assumptions with a deterministic multi-tier locator strategy that first scopes to visible tracking modal roots, then resolves fields by semantic cues (label/name/placeholder), then falls back to guarded selectors.

    Enforce required-field contract before submit:
    - Tracking number must resolve to a visible editable field and round-trip value check must pass.
    - Package contents field must resolve similarly.
    - If either required field fails, return explicit failure with stable machine-readable prefix (`FLAG_REQUIRED_FIELD_MISSING`) and actionable context.

    Keep retry budget bounded: max 2 attempts total, no unbounded waits.
  </action>
  <verify>
    Run: `venv/bin/python3 -m py_compile electronics_buyer.py`
    Expected: no syntax errors.
  </verify>
  <done>
    `submit_tracking` returns deterministic, explicit failure reasons for missing modal/fields and never loops indefinitely.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add duplicate blocking and explicit tracking outcome flags</name>
  <files>models.py, electronics_buyer.py, browser_agent.py</files>
  <action>
    Add explicit warning/flag capacity to EB tracking results (schema-safe optional field), then classify post-submit outcomes into stable categories:
    - Duplicate detected -> block + `FLAG_DUPLICATE_TRACKING`
    - Error state detected -> `FLAG_TRACKING_SUBMIT_ERROR`
    - Success signal detected -> success=true
    - Ambiguous/no signal -> deterministic failure with `FLAG_NO_SUCCESS_SIGNAL`

    Ensure browser agent returns these failures without masking Amazon data and without placeholder behaviors.
  </action>
  <verify>
    Run: `venv/bin/python3 -m py_compile models.py electronics_buyer.py browser_agent.py`
    Expected: no syntax errors.
  </verify>
  <done>
    Tracking duplicate/error/ambiguous states are clearly distinguishable in response payloads and logs.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add fast tracking contract regression script (sub-2-minute target)</name>
  <files>test_eb_tracking_contract.py</files>
  <action>
    Create a focused regression script that validates tracking outcome classification and runtime guardrails without long end-to-end loops.

    Script requirements:
    - Executes quickly (target under 2 minutes).
    - Verifies failure flag semantics for at least duplicate and missing-field scenarios using deterministic checks/helpers.
    - Prints PASS/FAIL with clear reason.
  </action>
  <verify>
    Run: `venv/bin/python3 test_eb_tracking_contract.py`
    Expected: exits 0 and reports PASS for contract checks.
  </verify>
  <done>
    A reusable fast-check script exists and validates tracking hardening invariants.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `venv/bin/python3 -m py_compile models.py electronics_buyer.py browser_agent.py test_eb_tracking_contract.py`
- [ ] `venv/bin/python3 test_eb_tracking_contract.py` passes
- [ ] Tracking duplicate and missing-field scenarios return explicit machine-readable flags
- [ ] Retry/timeout behavior is bounded to short iteration cycles
</verification>

<success_criteria>

- Required tracking fields must be present and filled before submit
- Duplicate tracking is blocked and flagged
- Ambiguous outcomes do not report success
- Fast regression script validates contract behavior within short runtime budget
  </success_criteria>

<output>
After completion, create `.planning/phases/03-electronicsbuyer-submission-hardening/03-01-SUMMARY.md`
</output>
