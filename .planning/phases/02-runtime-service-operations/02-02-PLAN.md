---
phase: 02-runtime-service-operations
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - scripts/verify-runtime-operations.sh
  - docs/RUNTIME_VALIDATION.md
  - docs/INSTALL.md
  - docs/OPERATIONS.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - Operator can run a reproducible validation flow for start -> status -> logs -> stop behavior from a fresh-install perspective.
    - Runtime validation output clearly distinguishes pass/fail and points to exact next remediation command(s).
    - Installer and operations docs provide one coherent handoff into runtime validation.
  artifacts:
    - scripts/verify-runtime-operations.sh
    - docs/RUNTIME_VALIDATION.md
    - docs/INSTALL.md
    - docs/OPERATIONS.md
  key_links:
    - Validation script exercises service scripts rather than bypassing them, preserving command-surface integrity.
    - Runtime validation docs align with installer handoff and operations runbook.
---

<objective>
Validate runtime service operations from a fresh-install operator workflow.

Purpose: Prove Phase 2 command surface is reliable in real usage order and produces actionable outcomes.
Output: Runtime validation script + acceptance doc + installer/operations doc alignment.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-runtime-service-operations/02-CONTEXT.md
@.planning/phases/02-runtime-service-operations/02-RESEARCH.md
@.planning/phases/02-runtime-service-operations/02-01-SUMMARY.md
@scripts/services-up.sh
@scripts/services-down.sh
@scripts/services-status.sh
@scripts/services-logs.sh
@docs/INSTALL.md
@docs/OPERATIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add runtime operations verification harness</name>
  <files>scripts/verify-runtime-operations.sh</files>
  <action>Create an executable verification script that runs the canonical runtime path in order (`services-up`, `services-status`, bounded `services-logs`, `services-down`) and returns non-zero on blocking failures. Output should be concise, explicit, and include remediation pointers when a stage fails.</action>
  <verify>Run `bash -n scripts/verify-runtime-operations.sh`; run harness in at least one expected-pass and one expected-failure scenario (or simulated failure path) to confirm deterministic exit codes and actionable output.</verify>
  <done>Validation harness provides an objective pass/fail signal for runtime operations readiness.</done>
</task>

<task type="auto">
  <name>Task 2: Document runtime acceptance procedure for post-install operators</name>
  <files>docs/RUNTIME_VALIDATION.md</files>
  <action>Author a runtime validation guide that explains prerequisites, command sequence, pass/fail interpretation, and first-response troubleshooting. Keep focus on operator actions and expected script outputs, not implementation internals.</action>
  <verify>Follow the documented commands in order and confirm they match real script interfaces and output contracts.</verify>
  <done>Operators can validate runtime service operations with one deterministic documented flow.</done>
</task>

<task type="auto">
  <name>Task 3: Align installer and operations docs with runtime validation handoff</name>
  <files>docs/INSTALL.md, docs/OPERATIONS.md</files>
  <action>Update installer and operations documentation so runtime validation appears as the next step after setup/readiness checks. Ensure command paths are exact and consistent across docs.</action>
  <verify>Cross-check `docs/INSTALL.md` and `docs/OPERATIONS.md` for command consistency and explicit runtime validation handoff.</verify>
  <done>Documentation flow from install to runtime validation is coherent and unambiguous.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash -n scripts/verify-runtime-operations.sh`
- [ ] Validation harness executes canonical runtime command order and fails non-zero on blocking issues
- [ ] `docs/RUNTIME_VALIDATION.md` + `docs/INSTALL.md` + `docs/OPERATIONS.md` reflect one coherent runtime validation path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Runtime behavior is validated through script-driven acceptance flow
- Documentation presents one clear post-install runtime validation path
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-service-operations/02-02-SUMMARY.md`
</output>
