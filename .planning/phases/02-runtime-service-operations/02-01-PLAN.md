---
phase: 02-runtime-service-operations
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/service-env.sh
  - scripts/services-up.sh
  - scripts/services-down.sh
  - scripts/services-status.sh
  - scripts/services-logs.sh
  - ecosystem.config.js
  - docs/OPERATIONS.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - Operator can start required services with one stable command path and clear success/failure output.
    - Status command reports PM2 process state plus API and n8n health endpoint checks with explicit pass/fail semantics.
    - Logs command supports deterministic default output and service-specific retrieval for support workflows.
  artifacts:
    - scripts/services-up.sh
    - scripts/services-status.sh
    - scripts/services-logs.sh
    - ecosystem.config.js
    - docs/OPERATIONS.md
  key_links:
    - Service scripts source `scripts/service-env.sh` for repo-local PM2/n8n isolation and consistent defaults.
    - `ecosystem.config.js` enables n8n health endpoint behavior consumed by status checks.
    - `docs/OPERATIONS.md` documents exact runtime commands and expected operator-visible outcomes.
---

<objective>
Harden runtime lifecycle scripts and command UX for consistent daily operations.

Purpose: Satisfy Phase 2 requirement that operators can reliably start, observe, and troubleshoot services through one documented command path.
Output: Updated service scripts + PM2 config + operations documentation with explicit pass/fail and recovery semantics.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-runtime-service-operations/02-CONTEXT.md
@.planning/phases/02-runtime-service-operations/02-RESEARCH.md
@scripts/service-env.sh
@scripts/services-up.sh
@scripts/services-down.sh
@scripts/services-status.sh
@scripts/services-logs.sh
@ecosystem.config.js
@docs/OPERATIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize runtime command UX and failure contracts across service scripts</name>
  <files>scripts/service-env.sh, scripts/services-up.sh, scripts/services-down.sh, scripts/services-status.sh, scripts/services-logs.sh</files>
  <action>Refactor service scripts to share a consistent operator contract: clear status labels, deterministic usage/help output, fail-fast dependency checks, and non-zero exits on blocking failures. Keep copy-paste command paths unchanged (`bash scripts/services-*.sh`) and preserve repo-local isolation behavior.</action>
  <verify>Run `bash -n scripts/service-env.sh scripts/services-up.sh scripts/services-down.sh scripts/services-status.sh scripts/services-logs.sh`; run representative failure path (for example missing `pm2`) and confirm explicit remediation output with non-zero exit.</verify>
  <done>All runtime scripts expose consistent command UX and blocking-failure semantics aligned to phase decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Harden service status checks and PM2 runtime lifecycle defaults</name>
  <files>scripts/services-status.sh, scripts/services-up.sh, ecosystem.config.js</files>
  <action>Update status behavior to check both process and endpoint layers (PM2 + API health + n8n health endpoint) with clear pass/fail reporting. Add required n8n health endpoint enablement and reasonable PM2 lifecycle defaults in ecosystem config so health checks are deterministic and restart behavior is predictable.</action>
  <verify>Run `bash -n scripts/services-status.sh scripts/services-up.sh`; inspect `ecosystem.config.js` for n8n health endpoint enablement and lifecycle fields; execute status script in controlled state and confirm endpoint-level failure surfaces clearly.</verify>
  <done>Status output is two-layered and deterministic; PM2/n8n config supports reliable health visibility.</done>
</task>

<task type="auto">
  <name>Task 3: Update operations runbook to match hardened runtime command surface</name>
  <files>docs/OPERATIONS.md</files>
  <action>Revise operations documentation so it mirrors actual script behavior: startup, shutdown, status interpretation, logs usage, and first-line recovery actions. Include exact commands and expected output cues for non-technical/semi-technical operators.</action>
  <verify>Cross-check every command in `docs/OPERATIONS.md` against script interfaces; confirm no undocumented command variants are required for normal operations.</verify>
  <done>Operations docs are command-accurate and reflect the hardened runtime UX contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash -n scripts/service-env.sh scripts/services-up.sh scripts/services-down.sh scripts/services-status.sh scripts/services-logs.sh`
- [ ] Status script reports PM2 state + API + n8n endpoint results with explicit pass/fail signals
- [ ] `docs/OPERATIONS.md` documents one canonical command path for start/stop/status/logs
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Runtime command UX is deterministic and failure messages are actionable
- INST-03 support baseline is established for fresh-install validation
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-service-operations/02-01-SUMMARY.md`
</output>
