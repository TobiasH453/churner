---
phase: 01-installer-secure-bootstrap
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - install.sh
  - scripts/install/common.sh
  - scripts/install/preflight.sh
  - scripts/service-env.sh
  - scripts/services-up.sh
  - scripts/services-down.sh
  - scripts/services-status.sh
  - scripts/services-logs.sh
  - ecosystem.config.js
  - docs/INSTALL.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - Running `install.sh` performs deterministic preflight checks before making setup changes.
    - Preflight reports all blocking failures in one summary and exits non-zero on failure.
    - Manual-required steps are printed with exact command, working directory, and reason, then pause for user confirmation.
    - Service operations use repo-local PM2/n8n state and collision-safe defaults so another local repo remains unaffected.
  artifacts:
    - install.sh
    - scripts/install/common.sh
    - scripts/install/preflight.sh
    - scripts/service-env.sh
    - ecosystem.config.js
    - docs/INSTALL.md
  key_links:
    - `install.sh` sources helper scripts and executes preflight before bootstrap actions.
    - service scripts source `scripts/service-env.sh` so PM2 app names/ports/state are repo-scoped.
    - `docs/INSTALL.md` reflects the installer as canonical path, not parallel ad-hoc commands.
---

<objective>
Build a guided installer skeleton with aggregated preflight and explicit manual-step handoff UX.

Purpose: Establish the canonical setup entrypoint for non-technical/semi-technical macOS users.
Output: A runnable `install.sh` flow with reusable helper modules and install docs aligned to the script behavior.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-installer-secure-bootstrap/01-CONTEXT.md
@.planning/phases/01-installer-secure-bootstrap/01-RESEARCH.md
@README.md
@docs/OPERATIONS.md
@scripts/services-up.sh
@scripts/rebuild_py39_env.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create installer orchestrator and shared output primitives</name>
  <files>install.sh, scripts/install/common.sh, scripts/service-env.sh</files>
  <action>Implement `install.sh` with strict shell mode, deterministic step ordering, centralized status logging, and explicit phase sections (`preflight`, `bootstrap`, `summary`). Add shared helper functions in `scripts/install/common.sh` for consistent pass/fail output, logging path setup, and manual-pause messaging with command/path/reason contract. Add `scripts/service-env.sh` that standardizes repo-local PM2/n8n isolation defaults and names.</action>
  <verify>Run `bash -n install.sh scripts/install/common.sh scripts/service-env.sh` and ensure script exits cleanly for `--help` or equivalent dry entrypoint.</verify>
  <done>`install.sh` is executable, uses shared helpers, and emits consistent guided output structure with reusable isolation env setup.</done>
</task>

<task type="auto">
  <name>Task 2: Implement aggregated preflight checks with hard-fail policy and service isolation</name>
  <files>scripts/install/preflight.sh, install.sh, ecosystem.config.js, scripts/services-up.sh, scripts/services-down.sh, scripts/services-status.sh, scripts/services-logs.sh</files>
  <action>Create preflight checks for required binaries and baseline reachability needed for install path (for example: `bash`, `python3`, `pip`, `node`, `npm`, `pm2`, `curl`, `git`, and network probe endpoints required for dependency installation). Collect all failures, print one blocking summary, and return non-zero. Enforce hard failure on network-dependent checks per locked context decisions. Ensure runtime service scripts and PM2 config consume repo-local env defaults (PM2_HOME, N8N_USER_FOLDER, unique process names, and conflict-safe ports) so this repo does not mutate another repo's runtime state.</action>
  <verify>Run `bash -n scripts/install/preflight.sh scripts/services-up.sh scripts/services-down.sh scripts/services-status.sh scripts/services-logs.sh`; run `bash install.sh` on a controlled env and confirm multi-failure summary behavior before setup mutation.</verify>
  <done>Preflight produces one consolidated report, aborts install on blocking issues, and runtime operations are repo-scoped by default.</done>
</task>

<task type="auto">
  <name>Task 3: Publish canonical install flow documentation</name>
  <files>docs/INSTALL.md</files>
  <action>Write `docs/INSTALL.md` as the canonical setup path: one-command install, preflight interpretation, expected manual pauses, and next-step handoff messaging. Keep docs command-accurate with script output and explicitly mention that manual-required steps always include exact command, directory, and reason.</action>
  <verify>Cross-check `docs/INSTALL.md` command lines against `install.sh`; run markdown lint if available.</verify>
  <done>Install documentation matches actual installer flow and removes ambiguity for fresh-machine setup.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash -n install.sh scripts/install/common.sh scripts/install/preflight.sh`
- [ ] Installer preflight fails fast after aggregated reporting when prerequisites are missing
- [ ] `docs/INSTALL.md` references `install.sh` as canonical path with exact commands
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No secret values are printed by new installer output paths
- Preflight behavior aligns with Phase 1 locked decisions
</success_criteria>

<output>
After completion, create `.planning/phases/01-installer-secure-bootstrap/01-01-SUMMARY.md`
</output>
