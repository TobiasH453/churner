---
phase: 01-installer-secure-bootstrap
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - scripts/validate-env.sh
  - docs/ENVIRONMENT.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - Operator can bootstrap `.env` from a committed template without exposing secret values.
    - Env validation reports missing/invalid key names and remediation steps without printing values.
    - Secret onboarding remains file-based and guided, not interactive secret prompts.
  artifacts:
    - .env.example
    - scripts/validate-env.sh
    - docs/ENVIRONMENT.md
  key_links:
    - `docs/ENVIRONMENT.md` uses `.env.example` and `scripts/validate-env.sh` as the only supported bootstrap path.
    - `scripts/validate-env.sh` enforces required keys for both personal and business account routes.
---

<objective>
Implement secure, template-guided environment bootstrap and non-leaking validation.

Purpose: Satisfy secret-handling requirements while keeping setup easy for semi-technical users.
Output: A maintainable `.env.example`, a strict validator script, and onboarding docs that match locked decisions.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-installer-secure-bootstrap/01-CONTEXT.md
@README.md
@main.py
@amazon_scraper.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define committed environment template for v1 paths</name>
  <files>.env.example</files>
  <action>Create/update `.env.example` with required keys and safe placeholders for the runtime and both account routes (`amz_personal`, `amz_business`), plus optional keys clearly marked. Keep placeholders descriptive and non-secret; include comments only where they reduce operator confusion.</action>
  <verify>Run `rg -n \"AMAZON_|ANTHROPIC_|N8N_|TZ|EB_\" .env.example` and compare against keys referenced in runtime code/docs.</verify>
  <done>Template covers required runtime keys with no real secrets and no ambiguous placeholders.</done>
</task>

<task type="auto">
  <name>Task 2: Build env validator with redacted diagnostics</name>
  <files>scripts/validate-env.sh</files>
  <action>Implement `scripts/validate-env.sh` to check `.env` existence, required key presence, and basic format sanity (for example non-empty credentials, expected account_type values where applicable, and plausible URL formats). Output must mention key names only and remediation steps; never echo raw values.</action>
  <verify>Run `bash -n scripts/validate-env.sh`; run validator with a temporary incomplete `.env` and confirm clear key-level failures without value leakage.</verify>
  <done>Validator exits non-zero on invalid state and provides actionable, secret-safe output.</done>
</task>

<task type="auto">
  <name>Task 3: Document template-first secret onboarding</name>
  <files>docs/ENVIRONMENT.md</files>
  <action>Write environment onboarding guide with explicit sequence: copy template, edit file manually, run validator, then continue installer flow. Include clear examples of command, directory, and expected validator status (pass/fail) while reinforcing local-only secret handling.</action>
  <verify>Follow doc steps on a temporary `.env` file and confirm all commands exist and behave as described.</verify>
  <done>Non-technical operator can set up `.env` confidently using one deterministic path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash -n scripts/validate-env.sh`
- [ ] `.env.example` includes required keys used by runtime routes and auth modes
- [ ] Validator output is key-level and redacted (no secret values)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Secret onboarding stays template-guided and file-based
- Validation feedback is actionable for non-technical users
</success_criteria>

<output>
After completion, create `.planning/phases/01-installer-secure-bootstrap/01-02-SUMMARY.md`
</output>
