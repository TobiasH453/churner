---
phase: 01-installer-secure-bootstrap
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - .bundleignore
  - scripts/audit-bundle.sh
  - docs/PACKAGING_BASELINE.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - Default distribution inputs exclude secret-bearing files and persistent browser/session state.
    - Operators can audit bundle contents before sharing/distribution.
    - Exclusion policy is explicit, documented, and reusable by later distribution phase work.
  artifacts:
    - .bundleignore
    - scripts/audit-bundle.sh
    - docs/PACKAGING_BASELINE.md
  key_links:
    - `scripts/audit-bundle.sh` reads `.bundleignore` and fails when forbidden paths are detected in candidate bundle contents.
    - Packaging baseline docs define sensitive paths to never distribute.
---

<objective>
Establish enforceable package-exclusion rules for sensitive local state and credentials.

Purpose: Lock security baseline early so later distribution work cannot accidentally ship secrets/session artifacts.
Output: Ignore policy, audit script, and baseline docs for safe packaging.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-installer-secure-bootstrap/01-CONTEXT.md
@README.md
@docs/OPERATIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define canonical sensitive-path exclusion manifest</name>
  <files>.bundleignore</files>
  <action>Create `.bundleignore` with patterns that exclude secrets and local session state by default, including `.env`, browser profile directories, `.n8n` state, and local logs/artifacts that may contain sensitive data. Keep rules explicit and annotated for maintainability.</action>
  <verify>Run `rg -n \"env|browser-profile|n8n|logs\" .bundleignore` and confirm all required sensitive locations are covered.</verify>
  <done>Exclusion manifest exists and covers known sensitive assets for this codebase.</done>
</task>

<task type="auto">
  <name>Task 2: Add bundle audit utility enforcing exclusion policy</name>
  <files>scripts/audit-bundle.sh</files>
  <action>Implement a shell script that validates candidate bundle file lists against `.bundleignore` and fails fast when forbidden files/paths are present. Support auditing a target archive path or a generated file list. Keep output concise in terminal and actionable.</action>
  <verify>Run `bash -n scripts/audit-bundle.sh`; execute against a synthetic file list that includes `.env` and confirm blocking failure.</verify>
  <done>Audit utility is runnable and reliably blocks sensitive content from distribution candidates.</done>
</task>

<task type="auto">
  <name>Task 3: Document packaging baseline and security guardrails</name>
  <files>docs/PACKAGING_BASELINE.md</files>
  <action>Write baseline packaging guidance that references `.bundleignore` and `scripts/audit-bundle.sh`, lists never-ship paths, and explains expected operator checks before zip/script distribution. Keep scope to baseline policy; defer full release packaging workflow details to Phase 6.</action>
  <verify>Manual doc review ensures every "never ship" path exists in `.bundleignore` and has rationale.</verify>
  <done>Security packaging baseline is explicit and ready for downstream distribution phase reuse.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash -n scripts/audit-bundle.sh`
- [ ] `.bundleignore` excludes `.env`, browser profiles, and local runtime state
- [ ] Audit script fails on intentionally injected forbidden paths
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Sensitive-state exclusion is auditable and default-safe
- Phase 6 packaging work has a concrete security baseline to build on
</success_criteria>

<output>
After completion, create `.planning/phases/01-installer-secure-bootstrap/01-03-SUMMARY.md`
</output>
