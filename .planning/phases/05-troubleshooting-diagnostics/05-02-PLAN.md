---
phase: 05-troubleshooting-diagnostics
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - scripts/collect-diagnostics.sh
  - docs/TROUBLESHOOTING.md
  - docs/OPERATIONS.md
  - docs/INSTALL.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - Operator can run one diagnostics command that collects actionable support artifacts.
    - Diagnostics output excludes raw secrets and sensitive local session state by default.
    - Diagnostics instructions clearly state where artifacts are written and what to share with support.
  artifacts:
    - scripts/collect-diagnostics.sh
    - docs/TROUBLESHOOTING.md
    - docs/OPERATIONS.md
    - docs/INSTALL.md
  key_links:
    - Diagnostics script reuses runtime script command surfaces for status/log capture.
    - Troubleshooting runbook references diagnostics bundle as escalation path when first-response commands do not resolve issue.
---

<objective>
Implement a redaction-safe diagnostics collector and support handoff contract.

Purpose: Satisfy SUP-02 by enabling reproducible evidence collection without exposing secret values.
Output: `scripts/collect-diagnostics.sh` plus documentation updates for collection, artifact location, and support sharing workflow.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-troubleshooting-diagnostics/05-RESEARCH.md
@.planning/phases/05-troubleshooting-diagnostics/05-01-SUMMARY.md
@scripts/service-env.sh
@scripts/services-status.sh
@scripts/services-logs.sh
@scripts/verify-runtime-operations.sh
@scripts/verify-smoke-readiness.sh
@docs/TROUBLESHOOTING.md
@docs/OPERATIONS.md
@docs/INSTALL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deterministic diagnostics collector entrypoint</name>
  <files>scripts/collect-diagnostics.sh</files>
  <action>Implement a non-interactive script with clear usage/help output and deterministic defaults (output directory, timestamp naming, bounded log lines). Collect command outputs needed for support triage (service status, bounded service logs, selected environment metadata, tool versions, and relevant verification command outputs where safe).</action>
  <verify>Run `bash -n scripts/collect-diagnostics.sh`; run `bash scripts/collect-diagnostics.sh --help`; run a local collection pass and confirm output directory/manifests are created with stable structure.</verify>
  <done>Diagnostics collector runs with one command and produces a predictable artifact bundle for support use.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce redaction and exclusion rules</name>
  <files>scripts/collect-diagnostics.sh</files>
  <action>Add redaction filters for secret-bearing patterns (tokens/passwords/auth headers/cookies) and enforce explicit exclusion of raw `.env`, browser profiles, and session/auth state files. Ensure script captures sanitized derivatives only.</action>
  <verify>Inject representative secret-like strings into sample command output path and confirm generated diagnostics artifacts replace them with redaction markers; verify excluded paths are not present in the bundle.</verify>
  <done>Diagnostics collection is safe-by-default and aligned with repository security constraints.</done>
</task>

<task type="auto">
  <name>Task 3: Document diagnostics collection and escalation flow</name>
  <files>docs/TROUBLESHOOTING.md, docs/OPERATIONS.md, docs/INSTALL.md</files>
  <action>Document when to run diagnostics, exact command syntax, output artifact location, and what files to share for support. Integrate this as the escalation step after first-response troubleshooting fails.</action>
  <verify>Follow docs from a failure scenario to diagnostics command and confirm the flow ends with a concrete share path and rerun guidance.</verify>
  <done>Support escalation path is explicit and reproducible for non-technical users.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash -n scripts/collect-diagnostics.sh`
- [ ] `bash scripts/collect-diagnostics.sh --help` returns usage text and exit code `0`
- [ ] Collector output includes manifest + bounded logs + status metadata in deterministic structure
- [ ] Diagnostics artifacts do not include raw `.env`, browser profile/session files, or unredacted secret-like values
- [ ] `docs/TROUBLESHOOTING.md`, `docs/OPERATIONS.md`, and `docs/INSTALL.md` document diagnostics command and share path
</verification>

<success_criteria>

- All tasks completed
- SUP-02 is satisfied with a safe diagnostics collection workflow
- Troubleshooting and diagnostics now form one coherent support flow
</success_criteria>

<output>
After completion, create `.planning/phases/05-troubleshooting-diagnostics/05-02-SUMMARY.md`
</output>
